terraform {
  required_version = ">= 1.6.0"
}

# MVP scaffold only.
# No AWS resources are created yet, so no provider is configured in Sprint 1.
# Future iterations can add the AWS provider together with a free-tier-friendly
# deployment target such as S3 static hosting or a small container service.
